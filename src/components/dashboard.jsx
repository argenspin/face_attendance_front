import {React} from 'react'

function Dashboard(){
    return(
        <div>
            <h1 className='h2'>Welcome, Navigate to one of the sidebar links to start working</h1>
        </div>
    )
}

export default Dashboard;